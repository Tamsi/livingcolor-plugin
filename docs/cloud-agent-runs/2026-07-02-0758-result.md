---
status: partial
workflow_frt_status: partial
shadow_mode: false
created_pr: ""
sandbox_pr_url: "https://github.com/abecms/tv5mondeplus-front/pull/5"
sandbox_jira_ticket: "TVP-10"
hermes_version: "0.18.0"
hermes_plugin_enabled: true
hermes_gateway_ok: true
hermes_bootstrap: passed
setup: passed
settings: passed
integrations: passed
readiness: passed
gates: passed
delivery: passed
jira_writeback: partial
github_publication: passed
sprint_report: blocked
stripe: passed
credentials:
  OPENROUTER_API_KEY: configured
  JIRA_URL: configured
  JIRA_USERNAME: configured
  JIRA_API_TOKEN: configured
  GITHUB_TOKEN: configured
  GH_TOKEN: missing
  STRIPE_SECRET_KEY: configured
  GITLAB_PERSONAL_ACCESS_TOKEN: configured
  GITLAB_API_URL: configured
top_findings:
  - id: F1
    classification: bug
    severity: high
    summary: "mcp-atlassian jira_update_issue requires fields as a JSON string; estimate write-back failed until invoker sent json.dumps(fields)."
  - id: F2
    classification: setup gap
    severity: high
    summary: "hermes plugins enable livingcolor blocks cloud bootstrap on interactive tool-override prompt; fixed with --no-allow-tool-override."
  - id: F3
    classification: integration_gap
    severity: medium
    summary: "Hermes messaging unavailable in cloud; sprint report composes and bills but message delivery fails."
---

# LivingColor TVP Cloud FRT — 2026-07-02-0758

## Executive Summary

Hermes Agent v0.18.0 with LivingColor plugin v0.3.23 was bootstrapped on port 9119. The full TVP delivery workflow was exercised end-to-end via `scripts/cloud-workflow-frt.py` against live sandbox integrations (Jira TVP, GitHub `abecms/tv5mondeplus-front`, Stripe test mode).

**Outcome: `partial`.** Core delivery through human gates, heuristic analyst/planner/developer/publisher, GitHub PR publication on **TVP-10** (PR #5 → `preprod`), and Jira comment writeback succeeded. Sprint report message delivery remains blocked (no Hermes messaging home channel). Stripe test draft invoice was created (`in_1TofR71rbdMPTLCdxydElPME`).

**Self-healing:** Two plugin fixes shipped on branch `cursor/livingcolor-cloud-heal-4a7f1afd`:
1. Non-interactive Hermes plugin enable for cloud bootstrap.
2. Jira `originalEstimate` write-back via mcp-atlassian using JSON-string `fields` (complements prior `get_issue` payload parsing fix on `main`).

Trigger: push to `main` (`56899dc` — parse mcp-atlassian Jira issue payloads for estimate write-back).

## Workflow FRT Results (phase A–H)

| Phase | Step | Result | Evidence |
|-------|------|--------|----------|
| A | Hermes + plugin mount | **pass** | `hermes --version` → 0.18.0; plugin enabled; overview → 200 |
| B | setup-automation + TVP mapping | **pass** | `POST .../projects/TVP/setup-automation` → 200 |
| C | Jira / GitHub / Stripe live | **pass** | Jira TVP 200; GitHub repo 200; Stripe balance `livemode=false` |
| D | Readiness scan + promote | **pass** | TVP-10 (RD-1) → WO-1; sprint pinned via selection |
| E | Human gates | **pass** | G-1..G-4 approved (`cloud-agent:test`) |
| F | Analyst → publisher | **pass** | PR https://github.com/abecms/tv5mondeplus-front/pull/5 (`feature/TVP-10`) |
| G | Sprint report | **blocked** | Message composed; `status=failed` — no Hermes messaging home channel |
| H | Stripe billing | **pass** | Draft invoice `in_1TofR71rbdMPTLCdxydElPME` via heuristic billing |

## Hermes Bootstrap Result

| Check | Status |
|-------|--------|
| `hermes` CLI | v0.18.0 |
| Plugin sync | `~/.hermes/plugins/livingcolor` |
| Plugin enabled | yes (v0.3.23) |
| API host | `http://127.0.0.1:9119` |
| Overview endpoint | `/api/plugins/livingcolor/delivery/overview` → 200 |
| `uvx` (mcp-atlassian) | installed |
| Jira MCP warmup | 53 tools |

**Bootstrap blocker healed:** Initial `hermes plugins enable livingcolor` hung on interactive tool-override prompt. `cloud-bootstrap.sh` now uses `--no-allow-tool-override`.

## Environment And Context

- Automation run: `4a7f1afd`
- Heal branch: `cursor/livingcolor-cloud-heal-4a7f1afd`
- Base commit: `56899dc` (feature branch with estimate read fix + cloud heals)
- Runtime: `LIVINGCOLOR_SYNC_ORCHESTRATOR=1`, heuristic backends
- `LIVINGCOLOR_SHADOW_MODE`: unset (false)
- Credentials: written via `cloud_write_credentials.py` from automation prompt

## Integration Result (Jira + GitHub live sandbox)

| Integration | Status | Notes |
|-------------|--------|-------|
| Jira TVP | available + writable | TVP-10; delivery publication comment at 2026-07-02T09:21:59Z |
| GitHub sandbox | available + writable | PR #5, branch `feature/TVP-10` → `preprod` |
| Stripe test | available + writable | Customer `cus_UoI0Nju7A7luZM`; draft invoice created |
| MCP Jira | connected | mcp-atlassian via uvx |
| MCP GitHub | connected | server-github |

## Gate Validation Result

Four gates approved in sequence (`analysis_plan`, `code_review`, `merge_request_review`, `jira_update`). Workflow paused at each gate until explicit `POST /gates/{id}/approve` with `approvedBy: cloud-agent:test`. No auto-skip observed.

## Jira Writeback Result

**partial**

- **Comments/publication:** pass — Live Jira comment on TVP-10 confirms publication writeback (PR #5, target `preprod`). No `shadow_mode` skip.
- **Original estimate at Gate 1:** failed during FRT with `JIRA_ESTIMATE_WRITE_FAILED` — mcp-atlassian rejected dict `fields` (expects JSON string). Fixed in `lc_server/integrations/jira_estimate_invoker.py`; manual verification after fix sets `originalEstimate=1d` on TVP-10.

## GitHub Publication Result

**pass** — Real sandbox PR on `abecms/tv5mondeplus-front`:

- URL: https://github.com/abecms/tv5mondeplus-front/pull/5
- Branch: `feature/TVP-10`
- Target: `preprod`

## Sprint Report Result

**blocked** — Sprint report endpoint returned HTTP 200 with composed payload but delivery failed:

```
status=failed
reason=No enabled messaging platform with a home channel found in Hermes.
```

Invoice still generated (`in_1TofR71rbdMPTLCdxydElPME`). Hermes messaging is not available in the Cursor Cloud VM environment.

## Stripe Billing Result

**pass** — Test-mode Stripe validated:

- Balance probe: `livemode=false`
- Invoice: `in_1TofR71rbdMPTLCdxydElPME`
- Billing status: `draft_created`

## Secondary Test Results

| Suite | Result | Notes |
|-------|--------|-------|
| `tests/test_e2e_smoke.py` | 1 passed | |
| `tests/delivery_runtime/test_orchestration_phase2.py` | 4 passed | |
| `tests/delivery_runtime/test_delivery_api.py` | 16 passed, 2 failed | Pre-existing promote/gate timing (`awaiting_gate` vs `intake`) |
| `tests/lc_server/test_stripe_billing.py` | 2 passed | |
| `tests/lc_server/test_jira_estimate_invoker.py` | 1 passed | New — JSON-string fields for mcp-atlassian |
| `tests/delivery_runtime/test_gates_estimation_writeback.py` | 10 passed | |
| `ui` vitest | 162 passed, 2 failed | TZ-dependent caption in `use-daily-analysis.test.ts` (UTC cloud VM) |
| `ui` build | pass | |
| `python3 -m lc_server warm-skills-cache` | pass | |

**Ignored pytest suites** (`pytest.ini`):

- `tests/delivery_runtime/test_bn_shadow_evaluation.py`
- `tests/delivery_runtime/test_gate1_phase25_validation.py`
- `tests/delivery_runtime/test_live_evaluation.py`
- `tests/delivery_runtime/test_shadow_evaluation.py`

## Findings

| ID | Classification | Severity | Summary |
|----|----------------|----------|---------|
| F1 | bug | high | mcp-atlassian `jira_update_issue` requires `fields` as JSON string; estimate write-back failed until invoker fix |
| F2 | setup gap | high | Interactive `hermes plugins enable` blocks cloud bootstrap |
| F3 | integration gap | medium | Hermes messaging unavailable for sprint report delivery |
| F4 | regression | low | `test_delivery_api` promote/gate tests expect `awaiting_gate` at promote time |
| F5 | flaky test | low | vitest TZ-dependent time caption fails in UTC cloud VM |

## Self-Healing Changes

1. **`scripts/cloud-bootstrap.sh`** — `hermes plugins enable livingcolor --no-allow-tool-override` (non-interactive cloud bootstrap).
2. **`lc_server/integrations/jira_estimate_invoker.py`** — Prefer `json.dumps(...)` for `fields` on snake_case MCP update tools (mcp-atlassian).
3. **`tests/lc_server/test_jira_estimate_invoker.py`** — Regression test for JSON-string fields.

## Next Agent Instructions

1. Merge heal PR; re-run FRT to confirm `JIRA_ESTIMATE_WRITE_FAILED` no longer appears at Gate 1 on a fresh WO.
2. Sprint report remains `blocked` until Hermes messaging home channel is provisioned in cloud (or API accepts `blocked` with invoice-only success).
3. Consider aligning `test_delivery_api` promote assertions with sync orchestrator `intake` status.
4. Fix vitest TZ flake (`use-daily-analysis.test.ts`) with explicit timezone in test setup.

## Appendix

- FRT summary: `/tmp/lc-frt-summary.json`
- FRT evidence: `/tmp/lc-frt-evidence.jsonl`
- Hermes log: `/tmp/hermes-dashboard.log`
- WO-1 estimate failure event (pre-fix): `Could not update Jira originalEstimate for TVP-10: fields Input should be a valid string`
