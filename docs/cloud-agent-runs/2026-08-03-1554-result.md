---
status: partial
workflow_frt_status: passed
shadow_mode: false
created_pr: ""
sandbox_pr_url: "PR-7-on-sandbox-repo"
sandbox_jira_ticket: "TVP-13"
hermes_version: "0.19.0"
hermes_plugin_enabled: true
hermes_gateway_ok: true
hermes_bootstrap: passed
setup: passed
settings: passed
integrations: passed
readiness: passed
gates: passed
delivery: passed
jira_writeback: passed
github_publication: passed
sprint_report: blocked
stripe: passed
credentials:
  OPENROUTER_API_KEY: configured
  JIRA_URL: configured
  JIRA_USERNAME: configured
  JIRA_API_TOKEN: configured
  GITHUB_TOKEN: configured
  GH_TOKEN: missing
  STRIPE_SECRET_KEY: configured
  GITLAB_PERSONAL_ACCESS_TOKEN: configured
  GITLAB_API_URL: configured
top_findings:
  - id: F1
    classification: integration_gap
    severity: medium
    summary: "Hermes 0.19 MCP circuit breaker blocks Jira readiness via MCP; REST fallback added."
  - id: F2
    classification: integration_gap
    severity: medium
    summary: "Hermes messaging unavailable in cloud; sprint report composes and bills but message delivery fails."
  - id: F3
    classification: setup_gap
    severity: high
    summary: "Python mcp package was missing on fresh cloud VM; cloud-bootstrap now installs it explicitly."
---

# LivingColor TVP Cloud FRT — 2026-08-03-1554

## Executive Summary

Hermes Agent v0.19.0 with LivingColor plugin v0.3.23 was bootstrapped on port 9119. The full TVP delivery workflow was exercised end-to-end via scripts/cloud-workflow-frt.py against live sandbox integrations (Jira TVP, VCS sandbox, Stripe test mode). // pragma: allowlist secret

**Outcome: `partial`.** Core delivery through human gates, heuristic analyst/planner/developer/publisher, GitHub PR publication on **TVP-13** (PR #7 → `preprod`), and Jira writeback succeeded. Readiness scan used Jira REST fallback when Hermes MCP circuit breaker blocked MCP tool invokes. Sprint report message delivery remains blocked (no Hermes messaging home channel); Stripe test draft invoice was created (`in_1U0OAj1rbdMPTLCdF7ZU7bky`).

Trigger: push to `main` (`047755b` — CI audit + git-dependent Python tests fix).

## Workflow FRT Results (phase A–H)

| Phase | Step | Result | Evidence |
|-------|------|--------|----------|
| A | Hermes + plugin mount | **pass** | `hermes --version` → 0.19.0; plugin enabled; overview → 200 |
| B | setup-automation + TVP mapping | **pass** | `POST .../projects/TVP/setup-automation` → 200 |
| C | Jira / GitHub / Stripe live | **pass** | Jira TVP 200; GitHub repo 200; Stripe balance `livemode=false` |
| D | Readiness scan + promote | **pass** | TVP-13 (RD-3) → WO-1 via REST fallback scan |
| E | Human gates | **pass** | G-1..G-4 approved (`cloud-agent:test`) |
| F | Analyst → publisher | **pass** | PR #7 on sandbox repo (`feature/TVP-13`) |
| G | Sprint report | **blocked** | Message composed; `status=failed` — no Hermes messaging home channel |
| H | Stripe billing | **pass** | Draft invoice `in_1U0OAj1rbdMPTLCdF7ZU7bky` via heuristic billing |

## Hermes Bootstrap Result

| Check | Status |
|-------|--------|
| `hermes` CLI | v0.19.0 |
| Plugin sync | `~/.hermes/plugins/livingcolor` |
| Plugin enabled | yes (v0.3.23) |
| API host | `http://127.0.0.1:9119` |
| Overview endpoint | `/api/plugins/livingcolor/delivery/overview` → 200 |
| `mcp` Python package | installed (required for Hermes 0.19 stdio MCP) |
| `uvx` (mcp-atlassian) | installed |

## Environment And Context

- Automation run: `4a7f1afd`
- Heal branch: `cursor/livingcolor-cloud-heal-4a7f1afd`
- Base commit: `047755b` (main)
- Runtime: `LIVINGCOLOR_SYNC_ORCHESTRATOR=1`, heuristic backends
- `LIVINGCOLOR_SHADOW_MODE`: unset (false)

## Integration Result (Jira + GitHub live sandbox)

| Integration | Status | Notes |
|-------------|--------|-------|
| Jira TVP | available + writable | TVP-13; REST fallback for readiness; MCP invoke flaky under circuit breaker |
| GitHub sandbox | available + writable | PR #7, branch `feature/TVP-13` → `preprod` |
| Stripe test | available + writable | Customer configured; draft invoice created |
| MCP Jira | degraded | Connect reports tools; invoke blocked by circuit breaker without REST fallback |

## Gate Validation Result

Four gates approved in sequence (`analysis_plan`, `code_review`, `merge_request_review`, `jira_update`). Workflow paused at each gate until explicit `POST /gates/{id}/approve` with `approvedBy: cloud-agent:test`.

## Jira Writeback Result

**pass** — Delivery completed with publication stage; no `shadow_mode` skip in API responses.

## GitHub Publication Result

**pass** — Real sandbox PR:

- URL: PR #7 on sandbox repo
- Branch: `feature/TVP-13`
- Target: `preprod`

## Sprint Report Result

**blocked** — Sprint report endpoint returned HTTP 200 with composed payload but delivery failed:

```
status=failed
reason=No enabled messaging platform with a home channel found in Hermes.
```

Invoice still generated (`in_1U0OAj1rbdMPTLCdF7ZU7bky`).

## Stripe Billing Result

**pass** — Test-mode Stripe validated:

- Balance probe: `livemode=false`
- Invoice: `in_1U0OAj1rbdMPTLCdF7ZU7bky`
- Billing status: `draft_created`

## Secondary Test Results

| Suite | Result | Notes |
|-------|--------|-------|
| `tests/test_e2e_smoke.py` | 1 passed | |
| `tests/delivery_runtime/test_orchestration_phase2.py` | 4 passed | |
| `tests/delivery_runtime/test_delivery_api.py` | 15 passed, 3 failed | Pre-existing promote/gate timing with sync orchestrator |
| `tests/lc_server/test_stripe_billing.py` | 2 passed | |
| `ui` vitest | 164 passed | |
| `ui` build | pass | vite build OK |
| `lc_server warm-skills-cache` | pass | commit 316e3ba |

Ignored pytest suites (from `pytest.ini`):

- `tests/delivery_runtime/test_bn_shadow_evaluation.py`
- `tests/delivery_runtime/test_gate1_phase25_validation.py`
- `tests/delivery_runtime/test_live_evaluation.py`
- `tests/delivery_runtime/test_shadow_evaluation.py`

## Findings

| ID | Classification | Severity | Summary |
|----|----------------|----------|---------|
| F1 | integration_gap | medium | Hermes 0.19 MCP circuit breaker blocks readiness MCP invokes; REST fallback added |
| F2 | integration_gap | medium | Hermes messaging unavailable in cloud blocks sprint report delivery |
| F3 | setup_gap | high | Missing `mcp` Python package on fresh VM prevented MCP registration until bootstrap fix |
| F4 | regression | low | `test_delivery_api` promote/gate tests out of sync with sync orchestrator |
| F5 | bug | medium | `reconnect_mcp_server(name, cfg)` incompatible with Hermes 0.19 one-arg API |

## Self-Healing Changes

1. **jira_dashboard/mcp_compat.py** — Force-install 2-arg `reconnect_mcp_server` shim when Hermes 0.19 only exposes 1-arg upstream symbol.
2. **scripts/cloud-bootstrap.sh** — Always install Python `mcp` package; warmup Jira via LivingColor connect API inside dashboard process.
3. **jira_dashboard/service.py** — Skip redundant MCP reconnect; wait for tool registration; reset circuit breaker; invoke probe before reporting connected.
4. **lc_server/integrations/mcp_connect.py** — Same reconnect/wait pattern for generic MCP connect.
5. **lc_server/integrations/jira_rest_readiness.py** — Jira REST fallback (`/rest/api/3/search/jql`) for readiness when MCP unreachable.
6. **lc_server/integrations/jira_readiness.py** — Use REST fallback when MCP fetch fails.

## Next Agent Instructions

1. Investigate root cause of Hermes 0.19 MCP invoke failures after successful connect (circuit breaker trips on `_fetch_projects` despite probe success).
2. Consider Hermes messaging stub or webhook for cloud sprint report validation.
3. Update `test_delivery_api` promote/gate expectations for sync orchestrator behavior.
4. Pin Hermes version in cloud bootstrap docs (0.17 vs 0.19 behavioral differences).

## Appendix

- FRT summary: `/tmp/lc-frt-summary.json`
- Hermes dashboard log: `/tmp/hermes-dashboard.log`
- Work order: WO-1
- Readiness record: RD-3
