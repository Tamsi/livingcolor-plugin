---
status: partial
workflow_frt_status: partial
shadow_mode: false
created_pr: "https://github.com/abecms/livingcolor-plugin/pull/24"
sandbox_pr_url: "https://github.com/[REDACTED]/pull/5"
sandbox_jira_ticket: "TVP-10"
hermes_version: "0.19.0"
hermes_plugin_enabled: true
hermes_gateway_ok: true
hermes_bootstrap: passed
setup: passed
settings: passed
integrations: passed
readiness: passed
gates: passed
delivery: passed
jira_writeback: passed
github_publication: passed
sprint_report: blocked
stripe: passed
credentials:
  OPENROUTER_API_KEY: configured
  JIRA_URL: configured
  JIRA_USERNAME: configured
  JIRA_API_TOKEN: configured
  GITHUB_TOKEN: configured
  GH_TOKEN: missing
  STRIPE_SECRET_KEY: configured
  STRIPE_TEST_CUSTOMER_ID: configured
  GITLAB_PERSONAL_ACCESS_TOKEN: configured
  GITLAB_API_URL: configured
  LIVINGCOLOR_TEST_PROJECT_KEY: configured
  LIVINGCOLOR_TEST_GITHUB_REPO: configured
top_findings:
  - id: F1
    classification: regression
    severity: critical
    summary: "Hermes 0.19 + MCP 2.0 broke Jira tool calls (CallToolResult.isError vs is_error); fixed via mcp_compat patch."
  - id: F2
    classification: regression
    severity: high
    summary: "Hermes 0.19 upstream reconnect_mcp_server is 1-arg; plugin needs 2-arg config register — fixed via forced shim override."
  - id: F3
    classification: integration_gap
    severity: medium
    summary: "Hermes messaging unavailable in cloud; sprint report composes and bills but message delivery fails."
---

# LivingColor TVP Cloud FRT — 2026-08-11-1011

## Executive Summary

Hermes Agent **v0.19.0** with LivingColor plugin **v0.3.23** was bootstrapped on port **9119**. The full TVP delivery workflow was exercised end-to-end via `scripts/cloud-workflow-frt.py` against live sandbox integrations (Jira TVP, GitHub `[REDACTED]`, Stripe test mode).

**Outcome: `partial`.** Core delivery through human gates, heuristic analyst/planner/developer/publisher, GitHub PR publication on **TVP-10** (PR #5 → `preprod`), and Jira comment writeback succeeded. Sprint report message delivery remains blocked (no Hermes messaging home channel); Stripe test draft invoice was created (`in_1U3CdY1rbdMPTLCdZH4bRHkj`).

**Self-healing:** Two Hermes 0.19 / MCP 2.0 compatibility fixes in `jira_dashboard/mcp_compat.py` (see Self-Healing Changes).

Trigger: push to `main` (`33e4813` — skills lock bump to livingcolor-skills v0.2.1).

## Workflow FRT Results (phase A–H)

| Phase | Step | Result | Evidence |
|-------|------|--------|----------|
| A | Hermes + plugin mount | **pass** | `hermes --version` → 0.19.0; plugin enabled; overview → 200 |
| B | setup-automation + TVP mapping | **pass** | `POST .../projects/TVP/setup-automation` → 200 |
| C | Jira / GitHub / Stripe live | **pass** | Jira TVP 200; GitHub repo 200; Stripe balance `livemode=false` |
| D | Readiness scan + promote | **pass** | TVP-10 (RD-1) → WO-1; sprint pinned via selection |
| E | Human gates | **pass** | G-1..G-4 approved (`cloud-agent:test`) |
| F | Analyst → publisher | **pass** | PR https://github.com/[REDACTED]/pull/5 (`feature/TVP-10`) |
| G | Sprint report | **blocked** | HTTP 200; `status=failed` — no Hermes messaging home channel |
| H | Stripe billing | **pass** | Draft invoice `in_1U3CdY1rbdMPTLCdZH4bRHkj` via heuristic billing |

## Hermes Bootstrap Result

| Check | Status |
|-------|--------|
| `hermes` CLI | v0.19.0 |
| Plugin sync | `~/.hermes/plugins/livingcolor` |
| Plugin enabled | yes (v0.3.23) |
| API host | `http://127.0.0.1:9119` |
| Overview endpoint | `/api/plugins/livingcolor/delivery/overview` → 200 |
| `uvx` (mcp-atlassian) | installed |
| Jira MCP warmup | 67 tools |

## Environment And Context

| Item | Value |
|------|-------|
| Run ID | `bc-104bd763-1b6f-440f-a418-7db105b99b31` |
| Base commit | `33e4813` (main) |
| Heal branch | `cursor/livingcolor-cloud-heal-bc-104bd763` |
| `LIVINGCOLOR_SHADOW_MODE` | unset (false) |
| Orchestrator | `LIVINGCOLOR_SYNC_ORCHESTRATOR=1` |
| Backends | heuristic (analyst/planner/developer/publisher/sprint) |

## Integration Result (Jira + GitHub live sandbox)

| Integration | Status | Notes |
|-------------|--------|-------|
| Jira TVP | available | REST + MCP; readiness scan 19 tickets |
| GitHub sandbox | available | `[REDACTED]` write OK |
| Stripe test | available | `livemode=false`; draft invoice created |
| GitLab MCP | configured | not on critical path this run |

## Gate Validation Result

Work order **WO-1** paused at `awaiting_gate` between stages. Four gates approved in order:

1. `analysis_plan` (G-1)
2. `code_review` (G-2)
3. `merge_request_review` (G-3)
4. `jira_update` (G-4)

No auto-skip observed; sync orchestrator advanced only after each approval.

## Jira Writeback Result

Gate G-4 payload includes proposed Jira comment with MR URL. No `shadow_mode` skip reason in API responses. Ticket **TVP-10** used throughout.

## GitHub Publication Result

| Field | Value |
|-------|-------|
| Branch | `feature/TVP-10` |
| PR URL | https://github.com/[REDACTED]/pull/5 |
| Target | `preprod` |
| Shadow skip | none |

## Sprint Report Result

`POST /sprint/report?force=true` → HTTP 200, `status=failed`, reason: *No enabled messaging platform with a home channel found in Hermes.* Report composition and billing path still ran (invoice attached).

## Stripe Billing Result

| Field | Value |
|-------|-------|
| Invoice ID | `in_1U3CdY1rbdMPTLCdZH4bRHkj` |
| Billing status | `draft_created` |
| Mode | test (`sk_test_*`) |

## Secondary Test Results

| Suite | Result |
|-------|--------|
| `tests/test_e2e_smoke.py` | 1 passed |
| `tests/delivery_runtime/test_orchestration_phase2.py` | passed (included in batch) |
| `tests/delivery_runtime/test_delivery_api.py` | 2 failed (pre-existing: promote/gate timing vs sync orchestrator) |
| `tests/lc_server/test_stripe_billing.py` | passed |
| `tests/jira_dashboard/test_mcp_compat.py` | 2 passed |
| `ui` vitest | 164 passed |
| `ui` build | passed |
| `python3 -m lc_server warm-skills-cache` | OK |

**Ignored pytest suites** (`pytest.ini`): `test_bn_shadow_evaluation.py`, `test_gate1_phase25_validation.py`, `test_live_evaluation.py`, `test_shadow_evaluation.py`.

## Findings

| ID | Class | Severity | Summary |
|----|-------|----------|---------|
| F1 | regression | critical | MCP 2.0 `CallToolResult` uses `is_error`; Hermes 0.19 handler reads `.isError` → all Jira MCP calls failed until compat patch |
| F2 | regression | high | Upstream `reconnect_mcp_server(name)` incompatible with plugin's `reconnect_mcp_server(name, cfg)` for stdio MCP registration |
| F3 | integration_gap | medium | Hermes messaging home channel absent in cloud — sprint report delivery blocked |
| F4 | regression | low | `test_delivery_api` promote/gate tests expect `awaiting_gate` at promote time; sync orchestrator advances to intake |
| F5 | setup gap | info | `cloud-start.sh` fails when stdin is empty but not a TTY (use `cloud-bootstrap.sh` or `</dev/null`) |

## Self-Healing Changes

Branch: `cursor/livingcolor-cloud-heal-bc-104bd763`

1. **`jira_dashboard/mcp_compat.py`** — Force-override `reconnect_mcp_server` shim for Hermes 0.19 (2-arg config register).
2. **`jira_dashboard/mcp_compat.py`** — Patch `CallToolResult.isError` property for MCP 2.0 / Hermes 0.19 tool handler compatibility.
3. **`tests/jira_dashboard/test_mcp_compat.py`** — Unit tests for both compat layers.

## Next Agent Instructions

1. Merge heal PR after review; verify Hermes 0.19 remains supported on next Hermes release.
2. Consider pinning `hermes-agent<0.20` in cloud bootstrap until upstream fixes `isError` handling.
3. Sprint report messaging: configure a Hermes messaging home channel in cloud environment OR document `blocked` as expected for cloud FRT.
4. Optional: fix `cloud-start.sh` to skip stdin credential read when `</dev/null>` or non-TTY without KEY=VALUE lines.

## Appendix

- FRT summary: `/tmp/lc-frt-summary.json`
- FRT evidence: `/tmp/lc-frt-evidence.jsonl`
- Dashboard log: `/tmp/hermes-dashboard.log`
- Hermes dashboard: http://127.0.0.1:9119/livingcolor
